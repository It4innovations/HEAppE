============================================================
  HEAppE Middleware has been deployed!
============================================================

Release:   {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

Components deployed:
  ✓ HEAppE REST API
  ✓ DataStaging API
  ✓ SSH Agent (sidecar)
{{- if .Values.mssql.enabled }}
  ✓ Microsoft SQL Server
{{- end }}
{{- if .Values.vault.enabled }}
  ✓ HashiCorp Vault
  ✓ Vault Agent
{{- end }}
{{- if .Values.telemetry.enabled }}
  ✓ OpenTelemetry Collector
  ✓ Jaeger
{{- end }}

{{- if .Values.api.ingress.enabled }}

HEAppE API is accessible at:
{{- range .Values.api.ingress.hosts }}
  http{{ if $.Values.api.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}

Get the HEAppE API URL by running:
  export POD_NAME=$(kubectl get pods -n {{ .Release.Namespace }} -l "{{ include "heappe.api.selectorLabels" . | replace ": " "=" | replace "\n" "," }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl port-forward $POD_NAME 5000:80 -n {{ .Release.Namespace }}
  echo "HEAppE API: http://localhost:5000"
{{- end }}

{{- if .Values.telemetry.enabled }}

Jaeger UI (tracing):
  export JAEGER_POD=$(kubectl get pods -n {{ .Release.Namespace }} -l "{{ include "heappe.jaeger.selectorLabels" . | replace ": " "=" | replace "\n" "," }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl port-forward $JAEGER_POD 16686:16686 -n {{ .Release.Namespace }}
  echo "Jaeger UI: http://localhost:16686"
{{- end }}

For more information, visit: https://heappe.eu/docs
