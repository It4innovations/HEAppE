apiVersion: v1
kind: Service
metadata:
  name: {{ include "heappe.fullname" . }}-api
  labels:
    {{- include "heappe.labels" . | nindent 4 }}
    {{- include "heappe.api.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "heappe.api.selectorLabels" . | nindent 4 }}
